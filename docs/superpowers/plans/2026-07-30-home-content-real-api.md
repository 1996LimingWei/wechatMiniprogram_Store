# 首页内容真实化实施计划

## 验收目标

Issue #11 的七个首页接口在 `useMock: false` 下均从数据库读取数据；禁用内容和下架商品不返回；构建与自动化测试通过。

## 步骤

- [x] 新增内容表的增量迁移、空库初始化 DDL 和演示种子。
- [x] 新增内容数据对象、Mapper 和首页查询服务。
- [x] 改造 `AppIndexController`，移除首页对 `MockData` 的依赖。
- [x] 增加服务测试，覆盖排序、禁用过滤、下架商品过滤和空楼层。
- [ ] 执行迁移、接口验证、Maven 构建；更新状态、关闭 Issue #11 并提交推送。
