# 交易权限与 Mock 写操作边界实施计划

> 对应规格：[交易权限与 Mock 写操作边界设计](../specs/2026-08-01-trade-security-boundary-design.md)
> 建议分支：`feat/trade-security-boundary`

## 任务

- [x] 任务 1：管理员身份与权限
  - 使用环境配置注入的最小管理员账号、密码与 ID 生成独立管理员 Token，并赋予 `ROLE_ADMIN`；完整管理员表和后台账号管理后置。
  - 管理端发货、售后审批的日志记录真实管理员 ID。

- [x] 任务 2：收紧安全配置
  - `/admin-api/**` 改为 `hasRole("ADMIN")`；保留必要公开小程序读接口，私有接口不得再以路径整体放行。
  - 添加匿名、普通会员、管理员三类访问测试。

- [x] 任务 3：后端 Mock 写操作开关
  - 新增 `TradeMockActionProperties` 与统一守卫，不在 Controller 分散硬编码环境判断。
  - 配置 dev/test 显式开启、prod 强制关闭；三个 Mock 写接口统一接入。

- [ ] 任务 4：迁移与验收基线
  - 修复 `verify-db-migration.ps1`：构造包含首页内容基线的隔离库，按迁移目录实际版本数断言历史。
  - 更新 `verify-trade-flow.ps1` 使用测试管理员 Token，并增加安全边界断言。

## 完成门槛

1. 模块测试、全量构建通过。
2. Docker 隔离环境中迁移验证和交易自动验收通过。
3. 生产 profile 不存在可调用的 Mock 支付、发货、退款写入口。

## 后续顺序

本计划完成后，继续处理购物车物理删除/唯一键、退款订单-支付单-库存一致性、下单幂等与并发状态保护；随后再补全 Mock 页面和逐项真实数据源替换。

## Issue #21 实施记录

- 已实现管理员登录、管理员角色授权、管理端访问收紧和 Mock 写操作的生产环境强制禁用。
- 已通过交易模块测试和全量构建；Docker 恢复后仍需补充管理员 HTTP 鉴权、生产 profile 拒绝 Mock 写操作、迁移与交易自动验收。
