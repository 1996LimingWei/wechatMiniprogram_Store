# 首页运营入口排版修复计划

## 目标

- 修复首页 Banner 下方运营入口过多时图标和标题相互挤压的问题。
- 清理历史演示迁移产生的重复运营入口。
- 保证后台后续增加入口时，前端仍能按稳定网格自动换行。

## 实施步骤

1. 将运营入口区域改为最多五列的自适应网格。
2. 限制入口标题宽度并增加省略保护，图标统一填充容器。
3. 前端按跳转地址过滤重复入口，并为远程图标增加本地失败兜底。
4. 新增数据库迁移，停用已被正式入口替代的旧演示数据。
5. 执行迁移、接口、语法和数据库重放验证。
