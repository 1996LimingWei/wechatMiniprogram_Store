# ============================================
# 生产环境配置
# 敏感信息通过环境变量注入，不会硬编码在文件中
# 使用方式：--spring.profiles.active=prod
# ============================================

spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD}

# 微信小程序 - 通过容器/平台环境变量注入
wx:
  ma:
    appid: ${WX_MA_APPID:}
    secret: ${WX_MA_SECRET:}
    mock-enabled: false

wechat:
  pay:
    enabled: ${WECHAT_PAY_ENABLED:false}
    app-id: ${WECHAT_PAY_APP_ID:}
    mch-id: ${WECHAT_PAY_MCH_ID:}
    merchant-serial-no: ${WECHAT_PAY_MERCHANT_SERIAL_NO:}
    private-key-path: ${WECHAT_PAY_PRIVATE_KEY_PATH:}
    api-v3-key: ${WECHAT_PAY_API_V3_KEY:}
    platform-certificate-path: ${WECHAT_PAY_PLATFORM_CERTIFICATE_PATH:}
    notify-url: ${WECHAT_PAY_NOTIFY_URL:}

product:
  provider: database
  mock-endpoints-enabled: false

trade:
  mock-actions-enabled: false
  refund:
    provider: ${TRADE_REFUND_PROVIDER:disabled}
    sync-job-enabled: true
    sync-job-fixed-delay: ${TRADE_REFUND_SYNC_FIXED_DELAY:60000}
    sync-batch-size: ${TRADE_REFUND_SYNC_BATCH_SIZE:50}
  logistics:
    provider: ${TRADE_LOGISTICS_PROVIDER:disabled}

admin:
  auth:
    user-id: ${ADMIN_USER_ID:1}
    username: ${ADMIN_USERNAME:}
    password: ${ADMIN_PASSWORD:}

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

logging:
  level:
    com.shop: info
